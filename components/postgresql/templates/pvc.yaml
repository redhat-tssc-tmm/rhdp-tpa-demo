---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tpa-postgresql-data
  labels:
    app: tpa-postgresql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storage.size }}
