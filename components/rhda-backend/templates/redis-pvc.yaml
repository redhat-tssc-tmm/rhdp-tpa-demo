---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rhda-backend-redis
  labels:
    app: rhda-backend-redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.redis.storage }}
