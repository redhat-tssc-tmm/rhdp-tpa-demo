{{- if .Values.dbMonitor.enabled }}
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: tpa-db-monitor
  labels:
    app: tpa-db-monitor
spec:
  {{- if .Values.dbMonitor.routeHost }}
  host: {{ .Values.dbMonitor.routeHost }}
  {{- end }}
  to:
    kind: Service
    name: tpa-db-monitor
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
{{- end }}
