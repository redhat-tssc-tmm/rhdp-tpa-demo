---
apiVersion: v1
kind: Service
metadata:
  name: userinfo-dashboard
  labels:
    app: userinfo-dashboard
spec:
  selector:
    app: userinfo-dashboard
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
