---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: userinfo-dashboard-configmap-reader
  labels:
    app: userinfo-dashboard
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["list", "get"]
